## HTTP request logger

This is a simple tool to capture http requests to this server.


#### Flags

* `-listen` the host port to listen
* `-output-dir` the directory to store http requests in a text file